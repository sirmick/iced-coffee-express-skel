require('source-map-support').install()
child_process = require('child_process')
sys = require('sys')
chai =  require('chai')
path = require('path')

expect = chai.expect;


suite 'Logic layer', ->
  test 'stuff', (done) ->

    done()